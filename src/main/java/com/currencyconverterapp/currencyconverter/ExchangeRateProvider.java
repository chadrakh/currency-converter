package com.currencyconverterapp.currencyconverter;

public class ExchangeRateProvider {
    // This class communicates with the currency exchange API
    // and fetches the current rates through HTTP requests and
    // will also parse the response to get the necessary data
}
